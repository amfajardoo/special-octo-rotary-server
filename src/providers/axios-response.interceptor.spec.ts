import { AxiosResponseInterceptor } from './axios-response.interceptor';

describe('AxiosResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new AxiosResponseInterceptor()).toBeDefined();
  });
});
